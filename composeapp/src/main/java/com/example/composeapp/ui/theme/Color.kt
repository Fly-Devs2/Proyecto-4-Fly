package com.example.composeapp.ui.theme

import androidx.compose.ui.graphics.Color

// ── Backgrounds ──────────────────────────────────────────────────────────────
val BgDarkest  = Color(0xFF141029)   // Fondo de TODAS las pantallas
val BgDark     = Color(0xFF1A1535)   // Fondo secundario, bottom nav
val BgCard     = Color(0xFF1E1742)   // Cards, inputs, secciones
val BgSurface  = Color(0xFF262048)   // Headers con fondo, chips activos, superficies elevadas

// ── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFFFFFFF)   // Texto principal
val TextSecondary = Color(0xFFA8A2CC)   // Subtítulos, labels de campos
val TextMuted     = Color(0xFF6B6690)   // Placeholders, nav inactivos
val TextLight     = Color(0xFFF4F2FF)   // Texto destacado sobre fondos oscuros

// ── Accents ──────────────────────────────────────────────────────────────────
val AccentViolet      = Color(0xFF7C5CFF)   // Primario · rol Usuario · botones CTA · FAB Vender
val AccentVioletLight = Color(0xFFA78BFA)   // Acentos suaves, hover
val AccentGold        = Color(0xFFFFC23C)   // Rol Mensajero · badges (RARA/HOLO) · totales · títulos de sección
val AccentRed         = Color(0xFFFF6B6B)   // Rol Admin · errores · disputas · eliminar
val AccentMint        = Color(0xFF4AD8A8)   // Rol Tienda · éxito · entregado · "Gratis"
val AccentBlue        = Color(0xFF5BA8FF)   // Acento informativo (poco uso)
